/*

package lapr.project.utils;

import lapr.project.model.ship.ShipAtrib;
import lapr.project.model.ship.ShipMMSI;
import lapr.project.services.PairOfShips;
import org.junit.jupiter.api.Test;

import java.io.File;
import java.util.LinkedList;
import java.util.List;

import static org.junit.jupiter.api.Assertions.*;

class PairOfShipsOutputTest {

    @Test
    void outPut() {
     */
/*   List<ShipAtrib> list = new LinkedList<>();
        double d1 = 2.431;
        ShipAtrib ship3 = new ShipAtrib(563076200, 636015975, d1, d1, 808, 220.471, 836, 153.906);
        list.add(ship3);
        boolean a = PairOfShipsOutput.outPut(list);
        assertTrue(a);
        File f = new File("pairOfShips//" + "Ships_Pair.txt");
        boolean m = f.delete();
        assertTrue(m);
        boolean b =  PairOfShipsOutput.outPut(null);
        assertFalse(b, "The email shouldn't be sent!");
        File f1 = new File("pairOfShips//" + "Ships_Pair.txt");
        boolean n=f1.exists();
        assertFalse(n);
        List<ShipAtrib>emptyList=new LinkedList<>();
        boolean c =  PairOfShipsOutput.outPut(emptyList);
        assertFalse(c, "The email shouldn't be sent!");
        File f2 = new File("pairOfShips//" + "Ships_Pair.txt");
        boolean t=f2.exists();
        assertFalse(t);*//*

        
    }


}
*/
